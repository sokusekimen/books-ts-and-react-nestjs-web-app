import { StyledButton } from '../components/StyledButton';

## StyledButton

StyledButton はカスタムボタンコンポーネントです。`variant`でボタンの色を切り替えることができます。

- [Primary](#primary)
...

### Primary

```tsx
<StyledButton varint="primary">Primary</StyledButton>
```